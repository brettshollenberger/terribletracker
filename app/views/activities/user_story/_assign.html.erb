<% unless activity.trackable.nil? %>
  assigned <%= User.find(activity.information.to_i).decorate.full_name %> to <%= link_to activity.trackable.title, "/projects/#{activity.trackable.project.id}/user_stories/#{activity.trackable.id}", remote: true %>.
<% end %>
